import { InterviewShellComponent } from './interview-shell.component';

describe('InterviewShellComponent', () => {
  it('should be defined', () => {
    expect(InterviewShellComponent).toBeDefined();
  });
});
