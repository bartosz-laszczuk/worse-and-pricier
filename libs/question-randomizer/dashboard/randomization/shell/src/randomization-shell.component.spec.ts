import { RandomizationShellComponent } from './randomization-shell.component';

describe('RandomizationShellComponent', () => {
  it('should be defined', () => {
    expect(RandomizationShellComponent).toBeDefined();
  });
});
