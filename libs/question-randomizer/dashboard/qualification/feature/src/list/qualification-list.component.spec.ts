import { QualificationListComponent } from './qualification-list.component';

describe('QualificationListComponent', () => {
  it('should be defined', () => {
    expect(QualificationListComponent).toBeDefined();
  });
});
