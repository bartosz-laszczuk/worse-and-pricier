import { QuestionListComponent } from './question-list.component';

describe('QuestionListComponent', () => {
  it('should be defined', () => {
    expect(QuestionListComponent).toBeDefined();
  });
});
